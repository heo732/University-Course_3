<Window x:Class="WarehouseOfIndustrialGoods.Windows.SuppliesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseOfIndustrialGoods.Windows"
        mc:Ignorable="d"
        Title="Supplies" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <ListView Name="listViewSupplies" Margin="20">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="50" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Product" Width="100" DisplayMemberBinding="{Binding Product.Name}" />
                    <GridViewColumn Header="Supplier" Width="100" DisplayMemberBinding="{Binding Supplier.Name}" />
                    <GridViewColumn Header="Employee" Width="100" DisplayMemberBinding="{Binding Employee.Name}" />
                    <GridViewColumn Header="CountOfProduct" Width="100" DisplayMemberBinding="{Binding CountOfProduct}" />
                    <GridViewColumn Header="DateTime" Width="100" DisplayMemberBinding="{Binding DateTime}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>